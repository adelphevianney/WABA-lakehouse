"""Suite de tests du générateur WABA.

Ce fichier fait de `tests` un paquet : sans lui, pytest insère le répertoire
`tests/` dans sys.path et `tests.conftest` se résout vers un paquet homonyme
présent dans site-packages.
"""
